-- ad hoc polymorphism
-- commonly known as interfaces

Equatable : Type -> Constraint
Equatable t = {
    == : t -> t -> Bool
    != : t -> t -> Bool
}

Ordered : Type -> Constraint
Ordered t = Equatable t & {
    <= : t -> t -> Bool
}
