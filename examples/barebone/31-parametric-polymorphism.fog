-- parametric polymorphism
-- commonly known as generics

id : a -> a
id = x : a => x

num : Int32
num = id 5

f : Int32 -> (a -> a) -> Int32
f = n : Int32 => g : a -> a => g (addInt32 n 1)

g : a -> a
g = x : a => {
    y : a
    y = x

    y
}

main = printLine (toString ((id f) num (g id)))
