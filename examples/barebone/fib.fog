fib : Int32 -> Int32
fib = n =>
    if n == 0, 0
    if n == 1, 1
    else,      fib (n - 1) + fib (n - 2)

num1 : Int32
num1 = fib 5

num2 : Int32
num2 = fib 6


breaking x y =
    if x == 0,
        if y is A, 1,
        if y is B, 2,
      else, 3
