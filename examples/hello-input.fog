-- printPrompt : IO Unit
printPrompt = printLine "Enter your name: "

displayName = str : String => printLine (concatString "Hello, " str)

main = then printPrompt (bind readLine displayName)

-- main = then (printLine "Enter your name: ") (bind readLine printLine)

-- main = bind readLine printLine
